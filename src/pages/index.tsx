import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div className={
      `flex h-screen justify-center items-center`
    }>
      <span>Texto</span>
    </div>
  );
};

export default Home;
